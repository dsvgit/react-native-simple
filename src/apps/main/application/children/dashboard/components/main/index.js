import React from 'react';
import { View } from 'react-native';

import TodoWidget from '../../children/todoWidget/components/main';

export default props => {
  return (
    <View>
      <TodoWidget />
    </View>
  );
}
